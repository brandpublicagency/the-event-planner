
// Import the original BookingForm component from the component location
import BookingFormOriginal from "@/components/forms/BookingForm";

// Re-export it as the default export for the page
export default function BookingForm() {
  return <BookingFormOriginal />;
}
